node_modules
dist
.env
.env.local

# Capacitor / native build artifacts (the native project sources under ios/ and
# android/ MAY be committed once generated — but never these build outputs).
ios/App/Pods
ios/App/build
ios/App/App/public
ios/DerivedData
android/.gradle
android/build
android/app/build
android/app/src/main/assets/public
android/local.properties
# Signing secrets — never commit.
*.keystore
*.jks
android/keystore.properties
*.xcuserstate
.DS_Store
